# Core dependencies
pyinstaller>=5.6.2
setuptools>=42.0.0
wheel>=0.37.0

# Platform-specific build dependencies
pywin32>=300; sys_platform == 'win32'  # Required for Windows extensions and system integration
py2app>=0.28.0; sys_platform == 'darwin'  # Required for macOS .app bundling